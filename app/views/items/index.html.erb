<div class="banner-image">
  <div class="input-group flex-nowrap search-bar" style="width: 900px">
    <div class="input-group-prepend">
      <span class="input-group-text" id="addon-wrapping">Search</span>
    </div>
    <input type="text" class="form-control" placeholder="find what you are looking for" aria-label="text" aria-describedby="addon-wrapping">
  </div>
</div>

<div class="text-left mx-5 my-3">
  <h2>List of material</h2>
</div>
<div class="all-item-cards mx-1">
  <% @items.each do |item| %>

    <% if item.sold? %>
        <p>☑</p>
      <% else %>
        <div class="card" style="width: 18rem;">
          <%= cl_image_tag item.photo %>
          <div class="card-body d-flex flex-column justify-content-end">
            <h4><%= link_to item.title, item_path(item), class: "text-success"%></h4>
            <h5><%= item.description %></h5>
            <h5><%= item.price %>€</h5>
            <p><i class="fas fa-toolbox text-muted pr-2"></i><%= item.category %></p>
            <p><i class="fas fa-map-marker-alt text-muted pr-2"></i><%= item.location %></p>

          </div>
        </div>
      <% end %>



    <%#= link_to " 🗑", delete_path(task), method: :delete,
        data:{confirm: "are you sure?"}%>
  <% end %>
 </div>
<%#= link_to "+ create new task", tasks_new_path%>
