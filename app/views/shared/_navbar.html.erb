<div class="navbar navbar-expand navbar-lewagon">
  <div class="navbar-brand-name">
    <%= link_to root_path do %>
      Mat<strong>3R</strong>ial
    <% end %>
  </div>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav">
      <li class="nav-item active">
      <% if user_signed_in? %>
        <%#= cl_image_tag current_user.photo, class: "avatar" %>
        <div class="dropdown nav-item-dd active" >
          <p class="dropdown-toggle" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <i class="fas fa-user text-white pr-2"></i>
            <%= current_user.first_name %> <%= current_user.last_name %>
          </p>
          <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
            <a class="dropdown-item" href="#">
              My Profile
            </a>
              <a class="dropdown-item" href="items/new">
                Add listing
              </a>
            <%= link_to 'Log out', destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </div>

      <% else %>
        <%= link_to new_user_session_path do %>
         Log in
        <% end %>
      </li>
      <li class="nav-item active">
        <%= link_to new_user_registration_path do %>
          Sign up
        <% end %>
      </li>
      <% end %>

    </ul>
  </div>
</div>
