<%= link_to "Back", root_path %>
<div class= "results">
  <h2> These are the items that we found for you:</h2>
  <div class="all-item-cards mx-1">
  <% @items.each do |item| %>

    <% if item.sold? %>
        <p>☑</p>
      <% else %>
        <div class="card" style="width: 18rem;">
          <%= cl_image_tag item.photo %>
          <div class="card-body d-flex flex-column justify-content-end">
            <h4><%= link_to item.title, item_path(item)%></h3>
            <h4><%= item.description %></h4>
            <h5><%= item.price %>€</h5>
            <p><%= item.location %></p>
            <p><%= item.category %></p>
          </div>
        </div>
      <% end %>



    <%#= link_to " 🗑", delete_path(task), method: :delete,
        data:{confirm: "are you sure?"}%>
  <% end %>
  </div>
</div>
<!-- <div
  id="map"
  style="width: 50%;
  height: 600px;"
  data-markers="<%= @markers.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
></div>
 -->
